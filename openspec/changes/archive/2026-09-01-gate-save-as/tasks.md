# Tasks

- [x] Refuse Save As on the same condition as Save, above the file picker
- [x] Report a lone refusal at the point it happens; keep the batch reporting once
- [x] Mirror the gate tests with `saveAs: true` cases, and pin that the refusal precedes the picker
- [x] Pin that a lone refusal is not replayed into the next unrelated save
- [x] Rewrite the `docs/MISSING_FEATURES.md` Save As row
- [x] Spec delta inverting the *Saving elsewhere* scenario
- [x] File the uncovered gaps as their own issues — #146, #147, #148 (three of them worse than this one), #149

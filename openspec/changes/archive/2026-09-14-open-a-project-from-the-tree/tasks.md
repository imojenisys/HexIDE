# Tasks

- [x] 1. `ProjectDocumentViewModel` renders a `ProjectDefinition` read-only and tracks it live
- [x] 2. `ProjectDocumentView` plus its `ViewLocator` registration
- [x] 3. `IEditorService.EditProject`, identifying the document by project instance rather than by path
- [x] 4. `ProjectToolViewModel.OpenSelected` opens a project node instead of toggling it
- [x] 5. Tests, including that expansion no longer moves (mutation-checked both directions)
- [x] 6. Localization keys added to `en` and translated into all 29 shipped packs
